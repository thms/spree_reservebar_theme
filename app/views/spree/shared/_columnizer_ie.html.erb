<!--[if IE]>

	<style type="text/css">
		div#top-menu > ul > li > ul > div > li > a {color:#C13828;font-family:'MrsEaves', serif;font-size:20px;line-height:1.2em;}
		div#top-menu > ul > li > ul > div > li > ul > li > a {font-weight:bold;}

		.taxons-list .column {margin:0;}

		div#top-menu > ul > li > ul {-webkit-column-count: 0;-moz-column-count: 0;column-count: 0;}
	</style>

	<script type="text/javascript">
		$(function(){

			//$('#top-menu > .taxons-list > li > .taxons-list > li').addClass("dontsplit");
			$('.type\\/gifts li').addClass("dontsplit");
			$('.type\\/spirits li').addClass("dontsplit");
			$('.type\\/wine li').addClass("dontsplit");
			$('.type\\/craft-beer li').addClass("dontsplit");
			$('.type\\/experiences li').addClass("dontsplit");

			//$('.taxons-list  ul').columnize({columns:5, buildOnce : true, lastNeverTallest : true});
			$('.type\\/gifts  ul').columnize({columns:5, buildOnce : true, lastNeverTallest : true});
			$('.type\\/spirits  ul').columnize({columns:5, buildOnce : true, lastNeverTallest : true});
			$('.type\\/wine  ul').columnize({columns:5, buildOnce : true, lastNeverTallest : true});
			$('.type\\/craft-beer  ul').columnize({columns:5, buildOnce : true, lastNeverTallest : true});
			$('.type\\/experiences  ul').columnize({columns:5, buildOnce : true, lastNeverTallest : true});
		});
	</script>
<![endif]-->


